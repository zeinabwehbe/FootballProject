import { NextFunction, Request, Response } from 'express';
import { ExpressYupMiddlewareInterface } from './schema-validation-interface';
export declare const expressYupMiddleware: ({ schemaValidator, expectedStatusCode, propertiesToValidate, }: {
    schemaValidator: ExpressYupMiddlewareInterface;
    expectedStatusCode?: number;
    propertiesToValidate?: string[];
}) => (req: Request, res: Response, next: NextFunction) => Promise<void | Response<any, Record<string, any>>>;
